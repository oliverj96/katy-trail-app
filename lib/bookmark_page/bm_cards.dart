import 'package:flutter/material.dart';

class BMCard extends StatelessWidget {
  const BMCard({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // child: child,
    );
  }
}